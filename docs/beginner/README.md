# Beginner

These pages are for users who are still getting comfortable with HIVE, SLURM, and shared cluster computing.

- [Using GPUs on HIVE](./gpu_on_hive.md): what GPUs are useful for, how to request them, and when to use the `low` partition instead of `gpu-a100`.
